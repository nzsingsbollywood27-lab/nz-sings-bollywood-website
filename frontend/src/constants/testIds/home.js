// Test IDs for the home / landing feature. Naming follows the directive
// in ./auth.js: keys use camelCase, values use kebab-case `<feature>-<element>`.

export const HOME = {
    homeLink: "home-link",
};